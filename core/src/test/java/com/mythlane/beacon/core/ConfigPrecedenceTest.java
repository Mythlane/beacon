package com.mythlane.beacon.core;

import org.junit.jupiter.api.Test;

import org.junit.jupiter.api.Disabled;

// Covers FND-02 (config precedence: env > file > default); implemented by Plan 02.
class ConfigPrecedenceTest {
    @Test
    @Disabled("Wave 0 stub — implementation pending")
    void pending() {
        // implementation lands in a later Plan
    }
}
